# Open — real domain and OG Worker

**Status:** deferred (do not block M0/M1)  
**PRD:** §15, F24  
**TECHSPEC:** no Worker, static title only

## Domain

Buy / point a domain only when the store listing or sharing actually needs one. Until then Pages preview or localhost (`VITE_PUBLIC_ORIGIN`) is enough.

## OG / previews

M0/M1: `document.title = Comments on {url}`. No OG Worker.

A later Worker is worth it only after people share thread links and unfurls look blank. Not a v1 promise.
